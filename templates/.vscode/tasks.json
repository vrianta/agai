{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build: prepare and compile",
            "type": "shell",
            "command": "bash",
            "args": [
                "-lc",
                "set -e && mkdir -p build && \n[ -d views ] && cp -R views build/ || true && \n[ -d templates/css ] && cp -R templates/css build/ || true && \n[ -d templates/js ] && cp -R templates/js build/ || true && \n[ -d static ] && cp -R static build/ || true && \n[ -d public ] && cp -R public build/ || true && \n# build the Go binary into build/ using workspace folder basename as name\ngo build -o build/${workspaceFolderBasename} ."
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            }
        }
        ,
        {
            "label": "run: execute built binary",
            "type": "shell",
            "command": "bash",
            "args": [
                "-lc",
                "if [ -x build/${PWD##*/} ]; then ./build/${PWD##*/}; else echo 'Binary not found. Run build task first.' >&2; exit 1; fi"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "none",
                "isDefault": false
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            }
        }
    ]
}
